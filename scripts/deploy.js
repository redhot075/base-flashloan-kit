const hre = require("hardhat");\n\nasync function main() {\n  const providerAddr = "0x..."; // Aave PoolAddressesProvider Base Sepolia\n  const BaseFlashLoan = await hre.ethers.getContractFactory("BaseFlashLoan");\n  const baseFlashLoan = await BaseFlashLoan.deploy(providerAddr);\n  await baseFlashLoan.waitForDeployment();\n  console.log("Deployed to:", await baseFlashLoan.getAddress());\n}\n\nmain().catch((error) => {\n  console.error(error);\n  process.exitCode = 1;\n});
